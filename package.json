{
  "name": "circuitcoder",
  "version": "1.0.0",
  "type": "module",
  "description": "Circuit Coder browser remake — build logic circuits, solve truth-table challenges",
  "scripts": {
    "test": "node --test tests/",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "serve": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
